using UnityEngine;

public class GameController : MonoBehaviour {

	// Use this for initialization
	void Start () {

	}
	
	// Update is called once per frame
	void Update () {
		// Add ambient sounds, disbable audio source and ambient source when not ingame, also stop music and play other one at differnt times
	}
}
